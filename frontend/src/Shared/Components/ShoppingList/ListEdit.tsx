/* eslint-disable react/prop-types */
import React from 'react';

const ListScreen = ({listId}) => {
  console.log(listId);
  return (
    <div>
      <div></div>
    </div>
  );
};

export default ListScreen;
